# Full stack (API + Streamlit) — single install surface
fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart
google-generativeai

streamlit
requests
pypdf

# Optional: local audio transcription (set IS_LOCAL=true + GCP creds)
google-cloud-speech
